
Partial Class about
    Inherits System.Web.UI.Page

    'Protected Sub Page_PreInit(sender As Object, e As EventArgs) Handles Me.PreInit
    '    Dim userMembership = Membership.GetUser(HttpContext.Current.User.Identity.Name)
    '    If userMembership IsNot Nothing Then
    '        If userMembership.IsApproved = True Then
    '            Me.MasterPageFile = "site.master"
    '        Else : Me.MasterPageFile = "~/Account/account.master"
    '        End If
    '    Else : Me.MasterPageFile = "~/Account/account.master" 'Me.MasterPageFile = "~/Account/account.master"
    '    End If
    'End Sub
End Class
